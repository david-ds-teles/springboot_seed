version: '3.0'
services:
  mysql:
    image: mysql:8
    restart: always
    environment:
      MYSQL_DATABASE: spring
      MYSQL_ROOT_PASSWORD: seed
    volumes:
    - ../../../scripts/db:/docker-entrypoint-initdb.d/